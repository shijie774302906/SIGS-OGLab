# 安全问题报告

如果发现密钥泄露、越权读取、数据意外上传、跨站请求、依赖漏洞或其他安全问题，请发送邮件至 `sigsoglab@163.com`。

邮件请尽量包含：

- 受影响的页面或功能；
- 可复现的最少步骤；
- 浏览器、操作系统和版本；
- 预期行为与实际行为。

请不要发送真实 API Key、未脱敏工程文件、身份证明或其他敏感数据。必要时请使用最小化的合成数据复现。

项目数据默认保存在浏览器本地。只有用户主动调用 AI 功能时，完成当前任务所需的内容才会发送至 AI 服务；API Key 不应进入项目存储、日志、截图或版本库。

收到报告后，维护者会先确认影响范围，再决定修复和公开说明。公开 Issue 不适合披露尚未修复的密钥或数据泄露细节。
