# 安全问题报告

如果发现密钥泄露、越权读取、数据意外上传、跨站请求、依赖漏洞或其他安全问题，请发送邮件至：

`sigsoglab@163.com`

邮件请包含：

- 受影响的页面或功能；
- 可复现的最少步骤；
- 浏览器、系统和版本；
- 预期行为与实际行为。

请不要发送真实 API Key、未脱敏工程文件、身份证明或其他敏感数据。必要时请使用最小化的生成数据复现。

当前项目在浏览器本地保存项目数据。DeepSeek API Key 只应存在于当前页面内存，并经本地 Node 转发服务逐请求发送；它不应进入项目存储、日志、截图或版本库。

收到报告后，维护者会先确认影响范围，再决定修复和公开说明。公开仓库的问题区不适合披露尚未修复的密钥或数据泄露细节。
