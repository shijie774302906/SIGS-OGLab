# Reference Manifest

This folder contains copied reference snapshots from `D:\CPT-UIQA`.

## Source Reference

`docs/source-reference/` contains selected project governance, current planning, UI rules, and legacy design notes.

These files preserve desktop context but should not be edited as if they were the active desktop repo.

## Design Reference

`docs/design-reference/` contains the VSCode-like workbench contract, Figma review records, and page handoff/planning documents.

Most important starting points:

- `docs/design-reference/ux-v5-vscode-like-workbench-contract.md`
- `docs/source-reference/design.md`
- `docs/design-reference/ui-05-stratification-default-planning-gate.md`

## Contracts

`docs/contracts/` contains copied domain and workflow contracts for:

- `LayerScheme`
- `ClassificationEvidence`
- `ParameterScheme`
- method registry/capability routing
- adopted output
- report/export gates

## Screenshots

`public/reference-screenshots/` contains copied PNG references. Use them to judge layout density and workflow shape, not as final approval.

## Sample Data

`sample_data/` contains copied JSON/CSV fixtures intended for frontend mock loading.

The larger raw Yingkou Excel/photo dataset was intentionally not copied during initialization.

