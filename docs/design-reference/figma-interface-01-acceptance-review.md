# Figma 界面 1 验收记录

日期：2026-07-03
Figma 文件：`Untitled`
范围：`01 Workbench shell`、`01A 新建工程弹窗`、`01B 打开工程弹窗`、`01C-1` 到 `01C-5 工程设置`

## 1. 总结论

界面 1 相关画板已经建立，且主体方向成立。

当前结论：

```text
界面 1 组：complete for v1 design / 第一版设计完成
```

含义：

- 可以认为 `01/01A/01B/01C` 已经进入验收阶段，不需要推倒重做。
- `01 Workbench shell` 的入口、菜单和默认态清理项已于本次修改完成。
- 01A、01B、01C 基本可用，主要是 nit 级 polish。

## 2. 顶层画板识别

真实设计稿：

| 节点 | 名称 | 判断 |
| --- | --- | --- |
| `1:408` | `01 Workbench shell` | 真实界面 1 主画板 |
| `1:672` | `01A 新建工程弹窗` | 真实入口弹窗 |
| `1:705` | `01B 打开工程弹窗` | 真实入口弹窗 |
| `1:733` | `01C-1 工程设置-基本信息` | 真实设置页 |
| `1:786` | `01C-2 工程设置-场址与基准` | 真实设置页 |
| `1:841` | `01C-3 工程设置-数据目录` | 真实设置页 |
| `1:894` | `01C-4 工程设置-本地数据库` | 真实设置页 |
| `1:954` | `01C-5 设计自检-工程设置必要性` | 设计自检说明页，不应作为用户主界面 |

参考图：

- `项目面板1-参考`
- `工程设计窗口-参考`
- `数据导入界面-参考`
- `数据检查界面-参考`
- `地层分类结果-参考`
- `成果输出窗口-参考`
- `新建工程窗口-参考`
- `地层分层模块对比-参考`
- `参数解译模块-参考`
- `参考方法选择模块-参考`

这些参考图不能直接算作已完成 UI，只能用于对照和后续吸收。

## 3. 01 Workbench shell 验收

截图证据：`app_data/temp/figma-01-workbench-shell.png`

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 顶部菜单 | pass | 已统一为 `文件/编辑/视图/转到/运行/输出/帮助` |
| Activity Bar | pass | 已收敛为真实全局入口：Explorer、问题、输出、成果、设置 |
| Explorer 工程树 | pass | `营口样例 -> CPT9-19-S1 -> 六步工作流` 成立 |
| Explorer 状态标签 | pass | `就绪/需检查/阻塞` 已出现，基本可读 |
| Explorer 操作区 | pass | 默认态已移除更多菜单浮层 |
| 中央默认页 | pass | 默认打开 `项目/点位数据`，且有工程结论摘要 |
| 新建/打开/导入入口 | pass | 页面右上有 `新建工程/打开工程/导入数据` |
| 右侧详情面板 | pass | 显示当前工程/点位摘要和下一步 |
| 底部面板 | pass | `质量问题/运行日志/方法对比/输出预检` 成立 |
| 状态栏 | pass | 显示当前文档、工程、点位、SQLite、本地模式等短状态 |
| VSCode-like 密度 | pass | 48 px Activity Bar、紧凑 Tab、280 px Explorer 基本符合 |

结论：

```text
01 Workbench shell：pass
```

已完成修复：

1. 顶部菜单统一，去掉 `选择`，将 `查看` 改为 `视图`。
2. Activity Bar 收敛为真实入口：Explorer、问题、输出、成果、设置。
3. 默认态移除 File 菜单预览和 Explorer 更多菜单浮层。
4. 顶部右侧从 `布局/视图/设置` 收敛为 `布局/面板`。

## 4. 01A 新建工程弹窗验收

截图证据：`app_data/temp/figma-01A-new-project.png`

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 中文化 | pass | 用户可见文案为中文 |
| 必要字段 | pass | 工程名称、编号、保存位置、海域、场址、坐标系、高程/水深基准均覆盖 |
| 按钮闭环 | pass | `取消`、`创建工程` 明确 |
| 布局对齐 | pass | 表单两列对齐清楚 |
| 状态表达 | nit | `待填写` 高亮合理，但可补必填/可选规则 |
| 样例复制入口 | nit | `从营口样例复制结构` 像按钮但状态不够明确，可改为复选框或次按钮 |

结论：

```text
01A：pass with nit
```

## 5. 01B 打开工程弹窗验收

截图证据：`app_data/temp/figma-01B-open-project.png`

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 中文化 | pass | 文案清楚 |
| 最近工程 | pass | 正常、未打开、只读、版本不兼容都有表达 |
| 本地文件夹 | pass | 有浏览入口和说明 |
| 按钮闭环 | pass | `取消`、`打开` 明确 |
| 工程软件感 | pass | 简洁，没有营销化 |
| 选中态 | nit | 最近工程的选中态可以更贴近 VSCode 列表 |

结论：

```text
01B：pass with nit
```

## 6. 01C 工程设置验收

截图证据：

- `app_data/temp/figma-01C1-settings-basic.png`
- `app_data/temp/figma-01C2-settings-site-datum.png`
- `app_data/temp/figma-01C3-settings-dirs.png`
- `app_data/temp/figma-01C4-settings-db.png`
- `app_data/temp/figma-01C5-settings-check.png`

| 子页 | 结果 | 说明 |
| --- | --- | --- |
| 基本信息 | pass | 工程元数据清楚，明确不改写 CPTU 曲线 |
| 场址与基准 | pass | 坐标/高程/水深基准边界清楚，符合后续二维到三维的需求 |
| 数据目录 | pass with nit | 目录用途清楚；迁移目录标为未来能力是对的，但需避免误认为可用 |
| 本地数据库 | pass with nit | 不展示开发路径；备份按钮灰显并解释，基本可接受 |
| 必要性与自检 | pass as design note | 已改名为 `01C-5 设计自检-工程设置必要性`，画板内明确“不进入用户实际界面” |

结论：

```text
01C：pass
01C-5：保留为设计自检页，不进入用户主界面
```

## 7. 最终判断

界面 1 不是“没做”，而是已经完成了主体结构。

推荐状态：

```text
01 Workbench shell：pass
01A 新建工程：pass with nit
01B 打开工程：pass with nit
01C 工程设置：pass
界面 1 整组：complete for v1 design
```

后续仅保留 nit polish：

1. `01A` 的 `从营口样例复制结构` 可改成复选框或次按钮，并说明复制范围。
2. `01B` 最近工程选中态可以进一步贴近 VSCode 列表。
3. `01C-3` 迁移目录保持未来能力，不进入主流程。
4. `01C-4` 创建备份保持灰显，并保留禁用原因。

## 8. 2026-07-03 修改后验证

Figma 修改：

- `1:413`：顶部菜单改为 `编辑   视图   转到   运行   输出   帮助`。
- `1:417`：顶部右侧改为 `布局  面板`。
- `1:426` 到 `1:429`：Activity Bar 改为 Explorer、问题、输出、成果。
- `1:411`、`1:418`、`1:437` 等菜单浮层节点已隐藏。
- `1:954`：画板改名为 `01C-5 设计自检-工程设置必要性`。
- `1:976` / `1:977`：自检页标题和说明改为“仅用于设计自检，不进入用户实际界面”。

验证截图：

- `app_data/temp/figma-01-workbench-shell-after-fix.png`
- `app_data/temp/figma-01C5-after-fix.png`

结构验证：

```text
menu rest = 编辑   视图   转到   运行   输出   帮助
layout = 布局  面板
MenuFileActive.visible = false
FileMenuPreview.visible = false
ExplorerMenu.visible = false
01C-5 frame name = 01C-5 设计自检-工程设置必要性
```
