# Active Plan

Date: 2026-07-31

Status: `Process139 active / confirmed`

## Process139：Vercel 私有预览与安全首发

### 确认卡

- 本轮目标：在不上传真实营口数据和任何密钥的前提下，建立私有 GitHub 仓库，并发布可访问的 Vercel 临时预览站。
- 用户真实动作：打开预览站；在浏览器内导入或粘贴数据；运行专业解译与快捷出图；导出 PDF/Excel；选择公共 DeepSeek 额度或输入自己的 DeepSeek Key 使用 AI 助手。
- 功能模块：安全发布清单、Vite 前端、Vercel AI Function、公共/自有 Key 路由、错误与重试、构建和部署配置。
- 页面承接：保留现有五区工作台和快捷出图；仅补齐线上 AI 连接状态及必要的恢复提示，不重做产品页面。
- 数据与接口：项目和工程数据继续保存在用户浏览器；公共 Key 仅存 Vercel 服务端环境变量；自有 Key 仅在当前浏览器会话中使用；首次验收只使用确定性生成数据。
- 验收成果：干净 Git 根提交、私有 GitHub 仓库、Vercel 临时 URL、机器安全审计、核心测试、双分辨率人工流程证据。
- 不做：不上传 `sample_data/source/yingkou`、`mishi.md` 或历史秘密；不公开仓库；不绑定正式域名；不加入访问统计数据库；不决定开源许可证；不把工程项目数据保存到云端。
- 关键风险：旧 Git 历史已跟踪营口工作簿，禁止直接推送；公共 AI 密钥不得进入前端包或 Git；Vercel 无共享状态，首次预览不声称具备精确的用户级额度统计。
- 已确认：用户已授权 GitHub/Vercel，确认首个预览版启用公共 DeepSeek 额度。

### 功能覆盖门禁

| 用户动作 | 对象与状态 | 页面承接 | 验收 |
| --- | --- | --- | --- |
| 打开预览站 | 正常加载 / 配置缺失 / 网络失败 | 现有入口与明确错误提示 | 页面可恢复，不出现白屏 |
| 导入生成数据 | 新建 / 替换 / 取消 / 格式错误 | 现有导入与快捷出图 | 原子导入，错误不污染旧数据 |
| 使用公共 AI | 可用 / 超时 / 服务失败 / 未配置 | AI 助手 | Key 不下发浏览器；失败可重试 |
| 使用自有 Key | 输入 / 验证失败 / 断开 / 刷新失效 | AI 助手 | 只在当前会话；不进入项目与 Git |
| 运行解译与出图 | 正常 / 部分结果 / stale | 五区工作流与图册 | 现有状态语义保持一致 |
| 导出结果 | 生成 / 取消 / 失败 / 重试 | 成果输出 | PDF、Excel 可下载且失败可重试 |
| 发布新版本 | 构建 / 部署失败 / 回滚 | GitHub + Vercel | 仅干净根提交进入远端 |

### 事件矩阵

| 事件 | 用户看到什么 | 可用动作 | 禁止继续条件 | 恢复 |
| --- | --- | --- | --- | --- |
| 公共 AI 未配置 | “公共 AI 暂不可用” | 输入自有 Key、稍后重试 | 仅 AI 请求 | 配置服务端变量后重新部署 |
| DeepSeek 超时或限额 | 简短原因，不丢当前页面与数据 | 重试、切换自有 Key | 当前请求 | 原地恢复 |
| 自有 Key 无效 | 验证失败 | 重新输入、断开 | 当前请求 | 当前会话内修正 |
| 导入异常 | 指出字段或格式问题 | 修改映射、取消、重试 | 数据确认 | 保留上一次有效草稿 |
| 部署构建失败 | Vercel 构建日志 | 修复后重新部署 | 新版本上线 | 旧部署继续可用 |
| 发现真实数据或密钥 | 安全检查失败 | 移除并重审 | 创建远端提交和部署 | 清理后重新生成干净快照 |

### 待办

- [ ] 运行 Known Problem Check，并纳入所有相关 required checks。
- [ ] 审计 Git 历史、工作树、真实数据、密钥、临时证据与发布边界。
- [ ] 添加发布忽略规则，生成无父提交、无真实数据、无密钥的干净发布快照。
- [ ] 创建私有 GitHub 仓库 `SIGS-OGLab` 并只推送干净根提交。
- [ ] 将本地 Node AI BFF 适配为 Vercel Function，公共 Key 只读服务端环境变量。
- [ ] 保持自有 Key 当前会话语义，并验证公共/自有两条 AI 路径。
- [ ] 部署临时 `vercel.app` 地址。
- [ ] 运行 build、目标测试、完整分层测试和发布安全检查。
- [ ] 用确定性生成数据验证导入、解译、快捷图册、PDF/Excel、AI 错误恢复。
- [ ] 本机运行至少一个不上传的真实全规模数据集，记录首次运行、切换、最长任务和持久化耗时；远端只保留不含工程数据的汇总结论。
- [ ] 验证运行中、禁用、完成、失败、取消、返回、连续失败、重试和恢复成功的顺序反馈。
- [ ] 验证错误提示包含原因、影响和准确恢复入口，修复后能续接原任务且不静默改写原始测量。
- [ ] 截图核对标签、单位、选中态、禁用态、溢出和错误日志；至少包含一次失败恢复。
- [ ] 完成 1440×900、1920×1080 检查以及控制台、溢出和密钥泄露检查。
- [ ] 生成精选证据、通过 Knowledge Gate、归档 Process139。

### 关闭标准

1. 远端提交的父节点为空，文件和历史均不含营口工作簿、`mishi.md`、API Key 或本地临时产物。
2. GitHub 仓库保持私有，Vercel 构建来源只指向该干净仓库。
3. 浏览器构建产物中不存在公共 DeepSeek Key；公共 AI 请求只经过同源服务端 Function。
4. 项目、点位、原始测量与解释结果不上传服务器。
5. 预览站核心流程与 AI 正常、失败、超时和重试路径均有证据。
6. 任何安全门禁失败时停止推送或部署，不以“后续再清理”关闭本切片。
