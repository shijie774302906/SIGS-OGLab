# Active Plan - Process153

Process: `Process153`

Date: `2026-08-04`

Status: `confirmed / implementation`

## Goal

从可发布代码、本机工作区和未来发布入口中清除临时领导页面、营口及其他私有工程数据、真实密钥与可再生成临时产物；用完全合成的通用样例替换前端运行时派生样例，并建立持续阻止误上传的安全门禁。

## Scope

- 删除 Process149 页面、测试和专属临时证据（若存在）。
- 删除营口原始文件、派生运行时样例和依赖私有文件的专属测试。
- 用确定性合成样例承接 `workflowData.ts`，不改变产品功能和工程算法。
- 删除本机 `mishi.md`、`.env/.env.local` 及可再生成 Playwright 临时目录；保留 `.env.example` 和精选关闭证据。
- 扩充 `.gitignore`、上线索引和发布审计测试。

## Acceptance

- 当前可发布树中不存在私有工程数据、真实密钥、Process149 页面或专属测试。
- `git ls-files`、发布审计和密钥扫描均通过。
- 合成样例可正常构建，领域/UI 测试通过。
- 删除项不会破坏现有首页、快捷出图、专业解译和生产部署。

## Todos

- [ ] 替换运行时私有派生样例并删除私有数据文件。
- [ ] 删除本机密钥和临时产物，保留精选证据。
- [ ] 加强忽略规则、上线索引和发布安全测试。
- [ ] 完成构建、分层测试、发布审计和知识门禁。
- [ ] 推送并重新部署正式站点，归档 Process153。

## Non-goals

- 不重写 Git 历史；若历史扫描发现需彻底擦除的对象，另行确认后执行历史重写。
- 不修改 `D:\CPT-UIQA` 桌面参考仓库。
- 不删除不含私有数据的历史流程说明和精选验收记录。
