# Active Plan - Process161 双站图册可读性发布

Status: `confirmed / in progress`

Date: 2026-08-18

## 目标

把 Process159/160 已验证的快捷 AI 协商导入与图册可读性改进，以同一构建产物发布到国际站 `sigs-oglab.com` 和国内站 `sigs-oglabx.com`，并完成线上双分辨率验收。

## 确认卡

### 功能定义

- 发布当前提交 `6c3282c` 所包含的正式产品功能。
- 两个正式站必须展示同一 Release Manifest 和同一前端行为。
- 不发布独立 AI 实验室、Process149 临时页面、私有工程数据、密钥、`.env` 或本机临时产物。

### 页面设计

- 不新增页面或改变工作流。
- 重点验证快捷与专业成果图册：11 pt 图例下限、长图例换行、提示与标题留白、长层名表格不截断。
- 保持现有导航、右侧工具栏和首次指引行为。

### 数据与接口

- 使用确定性公开/合成测试数据完成线上操作。
- 国际站与国内站继续使用既有同源 `/api`，不修改数据库、域名、DNS、SSL、配额或工程公式。
- 不上传或读取营口及其他私有工程数据。

### 验收方式

- `build:release`、`release:audit`、`release:parity`、`cloudbase:preflight` 全部通过。
- 两站 Release Manifest 的 Process、commit 和功能清单一致。
- 两站 API capabilities、访问统计与页面加载正常。
- 1440×900 与 1920×1080 下完成线上关键流程；无页面错误、控制台错误或横向溢出。
- 快捷和专业图册关键页可读，图例、标题、提示和长层名没有碰撞或截断。

## Feature Coverage Gate

| 用户动作 | 生命周期/事件 | 状态变化 | 页面承接 | 验收 |
| --- | --- | --- | --- | --- |
| 打开正式站 | 正常加载 | 进入首页 | 首页/导航 | 两站 200、无错误 |
| 生成快捷图册 | 新建→输入→生成 | 图册可浏览 | 快捷成果页 | 关键页截图、无溢出 |
| 查看专业成果 | 打开确定性项目→成果 | 图册可浏览 | 专业成果页 | A4/A3 关键页截图 |
| 请求线上接口 | 正常/失败/重试 | ready 或可解释错误 | AI/访问统计 | capabilities、visits 可访问 |
| 站点发布失败 | 失败/取消/恢复 | 旧版继续服务 | 托管平台 | 不切换失败产物 |
| 线上版本陈旧 | stale | manifest 不一致 | 发布门禁 | 一致性检查失败并停止 |
| 回滚 | 发布后异常 | 恢复上一稳定版 | Vercel/CloudBase | 使用 `pre-report-layout-process160` 或上一生产部署 |

## 待办

- [待办1] 建立发布清单并执行 Known Problem Check。
- [待办2] 完成构建、发布审计、安全和双站一致性门禁。
- [待办3] 将同一构建产物部署到国际站与国内站。
- [待办4] 完成双站、双分辨率 Playwright 与接口验收。
- [待办5] 晋级证据、更新发布索引并关闭 Process161。

## 非目标

- 不修改工程计算、分类、分层、参数解译或导出算法。
- 不引入新数据库、云函数、域名、证书或生产配额。
- 不发布实验智能体或私有数据。

## Known Problem Required Checks

- KPB-004：部署或线上请求失败时保留旧版，并验证取消、重试、连续失败与恢复成功路径。
- KPB-011：部署恢复必须回到对应站点/接口继续验收，不改写用户原始测量。
- KPB-037：发布前扫描 Git 跟踪文件与 `dist`，排除私有原始/派生数据、专属测试、密钥、临时页面及其标记；只用合成数据完成线上操作。
