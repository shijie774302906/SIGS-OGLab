# CPT Skills 高质量实施方案与研究计划

日期：2026-07-16

状态：`confirmed / research planning / implementation pending`

本文是 CPT/CPTU 专业 Skills 的独立研究总计划，不替代当前产品活动计划 `plan.md`，不改变 `Process107` 的范围和状态。后续 Skill 实施应在独立目录、独立评测记录和独立版本体系中进行。

## 1. 项目目标

将现有软件中已经形成和验证过的 CPT/CPTU 专业知识、工程解译顺序、计算方法、输入门槛、恢复规则和成果定义，抽象为可复用的 Codex Skills，使用户只需优先说明：

1. 最终目的是什么；
2. 需要处理的对象是什么；

Skill 随后负责检查输入、提出必要问题、选择可用知识、调用确定性脚本、按顺序产出成果，并保留完整来源与复现记录。

最终形成三套相互独立、共享基础设施的 Skills：

```text
cpt-interpretation
  原始数据 -> 数据检查 -> 派生计算 -> SBT -> 分层 -> 解译成果

cpt-parameter-inversion
  CPT/CPTU + 试验/参考数据 -> 参数反演 -> 独立验证 -> 敏感性与适用范围

cpt-method-reproduction
  规范/论文 -> 方法提取 -> 实现 -> 算例验证 -> 用户数据复现
```

第一期只实施并发布 `cpt-interpretation`。另外两套 Skill 在第一期完成研究协议和接口设计，在基础能力通过质量与效率验证后分阶段实施。

## 2. 核心研究问题

### RQ1：专业正确性

加入 Skill 后，Codex 是否能在不同 CPT/CPTU 输入条件下稳定遵循正确的单位、公式顺序、适用条件、人工确认边界和成果语义？

### RQ2：流程完整性

Skill 是否能把用户的目标转化为完整任务链，而不是只完成某一段计算？是否覆盖输入识别、问题定位、恢复、结果验证和成果输出？

### RQ3：效率增量

与相同模型、相同工具条件下不使用 Skill 的 Codex 相比，Skill 是否能减少完成时间、用户纠正、外部检索、重复代码和失败重跑？

### RQ4：泛化能力

Skill 是否能处理开发阶段未见过的列名、文件布局、异常组合、真实规模点位和用户表达，而不是记住固定样例？

### RQ5：可复现性

同一数据、Skill 版本、知识版本、脚本版本、设置和随机种子能否重现相同结果？结果差异能否追溯到具体输入或版本变化？

### RQ6：模块复用

工程解译 Skill 形成的数据、单位、验证、来源和成果基础，能否被参数反演与文献复现 Skill 直接复用，避免三套实现各自维护一份基础逻辑？

## 3. 研究假设

### H1：质量假设

在关键错误为零的前提下，使用 `cpt-interpretation` 的隐藏任务总体通过率不低于 90%，关键数值金值通过率为 100%。

### H2：效率假设

与无 Skill 基线相比，使用 Skill 的合格成果中位完成时间至少降低 30%，用户补充或纠正轮数至少降低 40%。

### H3：知识复用假设

对于已经植入并验证的方法，重复外部检索次数至少降低 70%，重复编写核心计算代码的次数至少降低 80%。

### H4：稳定性假设

相同版本和输入的可复现率达到 100%；跨模型运行时允许表达和规划不同，但确定性计算结果必须保持在相同容差内。

### H5：用户负担假设

Skill 只询问无法从输入推断且会实质改变结果的问题；与无 Skill 基线相比，无效或重复问题数量显著下降。

## 4. 项目范围

### 4.1 第一期范围：`cpt-interpretation`

覆盖：

- CSV、XLSX 等结构化 CPT/CPTU 文件识别；
- Sheet、表头、字段和点位识别；
- 字段映射与单位确认；
- 深度、qc、qt、fs、u2 等数据质量检查；
- 探头、面积比、地下水、孔压基准等必要条件检查；
- 经确认来源支持的 qt、qnet、Qt、Qtn、Fr、Bq、Ic 等派生量；
- 已确认规范或参考方法下的 SBT/SBTn 分类；
- SBT 图、深度曲线、分类结果和问题区间；
- 受约束的地层候选生成与连续性检查；
- 分层表、方法说明、问题清单和可复现运行清单；
- 无 u2、缺失面积比、异常行和方法不适用等降级或停止路径；
- 候选、试算、参考与工程确认状态的严格区分。

### 4.2 第二期范围：`cpt-parameter-inversion`

第一期只设计协议，第二期实施：

- CPT/CPTU 与室内/现场试验数据对齐；
- 反演对象、模型、目标函数和约束定义；
- 训练、验证和独立测试集划分；
- 参数优化、残差诊断和不确定性；
- 敏感性分析和跨点位泛化；
- 与规范推荐值、项目值和其他方法比较；
- 反演参数的适用范围、证据和禁止外推声明。

### 4.3 第三期范围：`cpt-method-reproduction`

第一期只设计协议，第三期实施：

- 从规范或论文中提取公式、表格、变量、单位和适用条件；
- 将论文未说明或有歧义的内容形成待确认假设；
- 生成版本化方法实现；
- 使用论文算例、独立金值或受控合成数据验证；
- 在用户数据上复现并解释差异；
- 保存引用、页码、实现哈希、测试和结果复现材料。

### 4.4 第一阶段非目标

- 不创建一个包含所有 CPT 研究方向的巨型 Skill；
- 不让 Agent 自由选择未经确认的规范公式；
- 不把模型文本推理当作关键数值计算权威；
- 不自动确定地下水、排水条件、正式土名或工程采用状态；
- 不把候选、试算或研究输出包装成正式工程成果；
- 不先实现任意 Python 插件市场或云协作平台；
- 不以单次演示、主观满意度或 token 减少作为唯一成功证据。

## 5. 总体架构

### 5.1 共享基础层

三套 Skill 共用以下基础资产：

```text
CPT Knowledge Base
  字段/单位、数据检查、规范方法、适用条件、术语和成果语义

CPT Deterministic Toolkit
  解析、校验、计算、分类、分层检查、统计、绘图和结果打包脚本

CPT Artifact Contracts
  输入快照、运行记录、曲线、分层、问题、来源和复现 manifest

CPT Evaluation Harness
  基线运行、任务执行、金值比较、事件记录、成本统计和评分
```

### 5.2 Skill 与资源职责

| 组成 | 职责 | 自由度 |
| --- | --- | --- |
| `SKILL.md` | 目标澄清、流程顺序、决策门槛、资源路由、停止条件 | 中等 |
| `references/` | 规范、公式来源、变量、单位、适用范围、术语和示例 | 中等 |
| `scripts/` | 关键计算、校验、绘图、统计和打包 | 低 |
| `assets/` | 输出 schema、报告模板、图表主题和示例结构 | 低 |
| Agent 推理 | 用户意图理解、异常解释、方案组织和结果叙述 | 高 |

### 5.3 第一套 Skill 目录建议

```text
cpt-interpretation/
├── SKILL.md
├── agents/
│   └── openai.yaml
├── references/
│   ├── input-fields-and-units.md
│   ├── data-quality-rules.md
│   ├── probe-water-and-pressure-context.md
│   ├── stress-and-normalization.md
│   ├── jts-methods.md
│   ├── robertson-sbt.md
│   ├── stratification-rules.md
│   ├── parameter-applicability.md
│   ├── terminology-and-status.md
│   └── output-contracts.md
├── scripts/
│   ├── inspect_input.py
│   ├── parse_cpt.py
│   ├── validate_profile.py
│   ├── calculate_derived_series.py
│   ├── classify_sbt.py
│   ├── propose_layers.py
│   ├── validate_layers.py
│   ├── render_results.py
│   └── build_run_manifest.py
└── assets/
    ├── result-schema.json
    ├── run-manifest-schema.json
    ├── interpretation-report-template.md
    └── figure-theme.json
```

详细知识只存在于对应 reference，不在 `SKILL.md` 重复。`SKILL.md` 应保持短小，明确告诉 Codex 在什么条件下读取哪一个 reference 或执行哪一个脚本。

## 6. 用户交互合同

### 6.1 最小入口

优先获取两个信息：

```text
最终目的：用户希望得到什么成果或判断？
处理对象：文件、点位、深度范围、项目或一组试验是什么？
```

### 6.2 提问规则

Skill 不应一次性提出完整问卷。按以下顺序处理：

1. 从输入文件和已有上下文提取可确定信息；
2. 执行只读预检；
3. 仅对会改变公式、适用性或成果语义的缺失信息提问；
4. 每次说明不同选择的影响和推荐理由；
5. 保存用户确认内容，不在同一运行中重复询问；
6. 用户拒绝确认时保留问题状态，不静默采用默认值。

### 6.3 必须人工确认的典型事项

- 目标规范或研究方法；
- 探头和面积比来源；
- 地下水、水深和压力基准；
- 无法由数据判定的排水条件；
- 数据异常的保留、修订或排除；
- 正式土名和工程分组确认；
- 候选分层是否仅供试算；
- 任何准备进入工程使用的人工参数或外部方法。

## 7. 评测体系

### 7.1 对照组

每项正式评测使用相同模型、相同 reasoning effort、相同工具、相同权限、相同输入和全新上下文：

```text
A 组：Codex，不加载 CPT Skill
B 组：Codex，加载待测 CPT Skill
```

如比较不同模型，采用完整因子设计：

```text
模型 × 是否使用 Skill × 任务类型 × 数据难度
```

不得让 A 组读取 Skill references、scripts 或预期答案；不得让 B 组看到隐藏任务答案。

### 7.2 任务集分层

| 数据集 | 用途 | Skill 作者是否可见答案 |
| --- | --- | --- |
| 开发集 | 建立初版流程和脚本 | 是 |
| 金值集 | 验证公式、边界和单位 | 输入可见，答案固定 |
| 回归集 | 每次 Skill 或知识更新运行 | 是 |
| 隐藏集 | 检验泛化，防止按样例特化 | 否 |
| 真实案例集 | 验证规模、数据噪声和完整流程 | 部分可见，由专家持有验收结论 |
| 对抗集 | 检验错误要求、安全边界和停止能力 | 测试设计者可见 |

### 7.3 第一期任务分类

#### T1 输入识别

- 标准英文 CSV；
- 中文列名 CSV；
- 多 Sheet XLSX；
- 表头不在第一行；
- kPa、MPa、Pa 混合表达；
- qc/qt、fs/ft、u2 的别名；
- 单文件多点位；
- 点位名称缺失或冲突。

#### T2 数据质量

- 深度递增；
- 重复深度；
- 深度逆序；
- 缺失值；
- 非有限数；
- qc、fs、u2 非法范围；
- 孤立异常；
- 连续异常区间；
- 原始值保护和调整记录。

#### T3 方法条件

- 完整 CPTU；
- 无 u2 的 CPT；
- 面积比缺失；
- 地下水条件不明确；
- 有效应力不可用；
- 目标方法不适用；
- 用户要求跳过必要检查。

#### T4 数值计算

- qt；
- qnet；
- 总应力和有效应力；
- Qt/Qtn；
- Fr；
- Bq；
- Ic；
- 迭代和收敛边界；
- 缺失输入传播；
- 单位边界和舍入。

#### T5 分类与分层

- SBT 区间边界；
- 不同深度间隔；
- 单层剖面；
- 多层剖面；
- 短薄层；
- 渐变区间；
- 无效区间断线；
- 最小层厚和端点约束；
- 分类证据、工程分组和正式土名语义分离。

#### T6 成果完整性

- SBT 图；
- 派生曲线；
- 分类表；
- 分层候选和分层表；
- 问题与警告；
- 方法、版本和来源；
- 用户确认项；
- 输入和结果哈希；
- 可复现运行说明。

### 7.4 金值策略

关键金值按可信度排序：

1. 已确认规范算例；
2. 仓库现有独立 JTS 金值；
3. 双人独立手工计算并复核；
4. 由明确公式生成的受控合成数据；
5. 经领域专家确认的真实案例结果。

每个金值记录：

- 输入；
- 单位；
- 方法与版本；
- 公式或表格来源；
- 中间结果；
- 期望结果；
- 绝对和相对容差；
- 边界包含规则；
- 复核人和时间。

## 8. 质量评分与硬门禁

### 8.1 一票否决项

出现下列任一问题，该次运行判定失败；正式候选 Skill 不得发布：

- 关键单位识别或换算错误；
- 公式实现或计算顺序错误；
- 未满足适用条件却继续计算；
- qc、qt、qnet、Qt、Qtn、Fr、Bq、Ic 概念混淆；
- 将 SBT 行为类型直接声明为正式土名；
- 缺少必要数据时静默填充工程默认值；
- 原始测量被静默修改；
- 无 u2 路径伪装为完整 CPTU 路径；
- 候选或研究结果伪装为工程确认成果；
- 参数反演训练和验证数据污染；
- 文献内容、页码或公式来源被虚构；
- 失败运行丢失或被标记为成功。

### 8.2 质量评分卡

| 维度 | 权重 | 主要证据 |
| --- | ---: | --- |
| 专业与数值正确性 | 35 | 金值、公式、单位和边界测试 |
| 流程与成果完整性 | 20 | 任务合同和产物清单 |
| 来源与可追踪性 | 15 | 引用、版本、哈希和 manifest |
| 安全边界与人工确认 | 15 | 对抗任务和取消/拒绝路径 |
| 可复现性 | 10 | 重跑、跨会话和版本锁定 |
| 用户操作清晰度 | 5 | 问题数量、有效性和恢复路径 |

硬门禁优先于总分。存在一票否决项时，不计算“高分通过”。

### 8.3 发布门槛

- 关键公式和单位错误：0；
- 原始数据静默修改：0；
- 未确认工程假设自动决定：0；
- 不适用方法静默运行：0；
- 金值通过率：100%；
- 来源可追踪率：100%；
- 相同版本与输入可复现率：100%；
- 普通输入首次成功率：至少 95%；
- 隐藏任务总体通过率：至少 90%；
- 异常输入正确停止或恢复率：至少 90%；
- 所有失败均给出原因、影响和恢复建议。

## 9. 效率评测

### 9.1 核心指标

| 指标 | 定义 |
| --- | --- |
| 首次成功率 | 首次运行无需人工纠正即满足验收的任务比例 |
| 合格成果时间 | 从任务开始到第一次满足全部验收条件的时间 |
| 用户轮数 | 用户补充、选择、纠正和确认的消息次数 |
| 无效问题数 | 可从输入确定或不影响结果却仍询问的问题 |
| 人工修正时间 | 专家发现并修正问题的时间 |
| 外部检索次数 | 为完成已植入方法而进行的网络或文档检索 |
| 工具调用数 | 文件读取、脚本、搜索、重跑和其他调用数 |
| 失败重跑数 | 获得合格结果前的失败或无效运行数 |
| 重复代码量 | 为已有计算临时生成的重复实现 |
| 成果完整率 | 要求产物一次性齐全的比例 |
| 可复现率 | 相同条件重跑得到一致结果的比例 |
| Token 使用 | 输入、缓存输入、输出和 reasoning token |
| 成本 | API 模式下按模型和 token 计算；订阅模式记录 credits/usage 指标 |

### 9.2 效率目标

相对无 Skill 基线：

- 合格成果中位时间减少至少 30%；
- 用户补充或纠正轮数减少至少 40%；
- 已植入方法外部检索减少至少 70%；
- 首次成功率提高至少 15 个百分点；
- 重复核心计算代码减少至少 80%；
- 人工检查和修正时间减少至少 30%。

效率目标只能在质量门禁通过后评价。更快地产生错误结果不构成提效。

## 10. 事件与观测合同

每次基线或 Skill 评测必须保存机器可读事件：

```text
run.started
goal.confirmed
input.inspected
question.asked
user.confirmed
reference.loaded
external.search
script.started
script.completed
tool.completed
artifact.created
validation.failed
validation.passed
run.completed
```

每条事件至少包括：

- run id；
- task id；
- group：baseline/skill；
- model；
- reasoning effort；
- Skill 名称和版本；
- 时间；
- duration；
- success/failure；
- 输入和输出 artifact id；
- token usage；
- 错误码；
- 是否需要用户参与。

敏感的用户原文和文件内容默认不进入遥测，只记录长度、哈希、类型和受控摘要。

### 10.1 Codex 评测采集实现

正式 A/B 和模型比较不以 VS Code 聊天界面的人工观察为主，采用可重复的非交互运行器：

```text
evaluation runner
  -> codex exec --json --model <model> <task>
  -> 保存完整 JSONL 事件
  -> 提取 turn.completed usage
  -> 提取 command/file/MCP/web/plan 等 item 事件
  -> 关联任务、Skill 版本、输入哈希和验收结果
  -> 生成 run-summary.json 和 comparison.csv
```

理由：

- `codex exec --json` 能稳定输出任务事件和 token usage；
- `--model` 可固定待测模型；
- `--output-schema` 可约束最终结果结构；
- 全新进程或 `--ephemeral` 可减少会话历史污染；
- 相同 runner 可以同时测试 Skill on/off；
- 自动化运行比在 IDE 中手工复制统计更容易复现。

交互式 VS Code 任务仍可用于真实用户体验测试。它的会话记录保存在 `$CODEX_HOME/sessions`，可只读提取模型、事件类型和累计 token；不得修改 Codex 原始 session 文件。

需要长期集中观测时，在用户级 Codex 配置中启用 OpenTelemetry。采集 API 请求时长、完成 token、工具决策、工具结果和错误事件；默认保持用户 prompt 内容关闭。遥测配置、密钥和出口不得提交到 Skill 或产品仓库。

### 10.2 成本核算边界

- API Key 运行：按模型的官方输入、缓存输入和输出 token 单价计算实际估算费用；保存当时使用的价格表版本。
- ChatGPT 登录：记录 token、rate limits、Fast/Standard 和 credits/usage 信息，但不把内部 token 简单换算成逐次美元费用。
- 不同登录方式的成本不可直接混为同一财务指标；报告中分别展示 API 成本和 ChatGPT usage/credit 消耗。
- reasoning effort、Fast mode、subagents 和工具循环必须作为实验变量记录，因为它们会改变时延和使用量。

## 11. 分阶段实施计划

### Phase 0：研究治理和目录隔离

目标：建立 Skill 项目的独立边界。

工作：

- 确定 Skill 创建位置和版本库；
- 建立研究数据、公开样例、私有真实数据的权限边界；
- 定义 Skill、知识、脚本、任务集和评测报告的版本号；
- 定义专家审查和变更批准方式；
- 建立敏感数据脱敏、日志和保留策略；
- 冻结本计划作为第一期研究合同。

交付物：

- 目录结构；
- 版本策略；
- 数据与隐私策略；
- 评测运行 schema；
- 变更和发布流程。

退出条件：研究运行不会修改当前产品数据或正式工程成果。

### Phase 1：无 Skill 基线

目标：证明普通 Codex 当前能做到什么、成本在哪里。

工作：

- 建立首批 15–20 个代表任务；
- 固定模型、reasoning effort、权限和工具；
- 使用全新会话执行无 Skill 基线；
- 保存完整事件、token、时间、用户轮数和成果；
- 由规则检查和领域专家独立评分；
- 建立主要失败模式目录。

交付物：

- baseline task set v1；
- baseline run records；
- baseline quality report；
- baseline efficiency report；
- failure taxonomy v1。

退出条件：每个目标指标均有可重复采集方法。

### Phase 2：知识库与金值

目标：先建立知识和数值权威，再写 Skill 流程。

工作：

- 从当前软件、JTS 合同和已确认资料中提取知识；
- 拆分字段单位、数据检查、应力归一化、SBT、分层和成果合同；
- 为每个公式记录来源、适用条件和验证状态；
- 建立数值金值、边界向量和异常传播向量；
- 将未确认或存在争议的方法明确排除或标记；
- 由领域专家复核知识与金值。

交付物：

- references v1；
- formula registry v1；
- golden vectors v1；
- terminology contract v1；
- knowledge review report。

退出条件：所有准备放入 Skill 的关键公式都有来源和独立验证。

### Phase 3：确定性脚本工具箱

目标：把易错、重复和需要稳定性的任务从模型推理移入脚本。

工作：

- 输入检查和格式探测；
- 标准化解析和单位转换；
- 数据质量检查；
- 派生量计算；
- SBT 分类；
- 分层候选与约束校验；
- 图表和成果生成；
- manifest 和哈希生成；
- 单元、性质、金值和大数据测试。

交付物：

- deterministic toolkit v1；
- script input/output schemas；
- script tests；
- performance report；
- failure codes。

退出条件：关键数值金值 100% 通过，脚本失败不会生成半完成成果。

### Phase 4：`cpt-interpretation` Skill 初版

目标：把用户目标、专业流程、references 和 scripts 连接起来。

工作：

- 使用 `skill-creator` 初始化 Skill；
- 编写精简 `SKILL.md`；
- 设计明确触发描述；
- 定义目标和对象的最小入口；
- 定义 reference 路由和脚本调用顺序；
- 定义提问、取消、错误、重试和停止路径；
- 生成并校验 `agents/openai.yaml`；
- 运行 Skill 结构校验。

交付物：

- `cpt-interpretation` v0.1；
- Skill 使用示例；
- Skill 结构验证结果；
- 初版端到端运行记录。

退出条件：开发集基本任务完整运行，未出现一票否决问题。

### Phase 5：前向测试与迭代

目标：验证 Skill 是否能被没有隐含背景的新 Agent 正确使用。

工作：

- 使用全新会话执行真实任务；
- 只提供用户任务、Skill 和必要原始输入；
- 不泄露期望答案和设计意图；
- 收集错误触发、漏读 reference、错误脚本选择和成果缺失；
- 根据失败修改 Skill 或资源；
- 每次修改后重跑完整回归集。

交付物：

- forward-test records；
- Skill v0.2–v0.x；
- 失败根因和修复映射；
- 回归报告。

退出条件：连续两轮前向测试无新增一票否决问题。

### Phase 6：正式 A/B 与隐藏测试

目标：证明 Skill 有质量和效率增量。

工作：

- 冻结待测 Skill 版本；
- 随机化任务顺序；
- 执行 baseline/skill 对照；
- 在隐藏集和真实案例集上执行；
- 对结果进行盲评；
- 统计质量、效率、token、成本和用户负担；
- 分析不同任务类型和难度下的增益；
- 必要时比较不同模型。

交付物：

- A/B dataset；
- hidden-test report；
- statistical summary；
- model × skill comparison；
- 质量和效率结论。

退出条件：发布门槛和效率目标同时通过，或形成明确的未通过原因。

### Phase 7：专家审查与 v1 发布

目标：形成可持续使用和更新的正式版本。

审查视角：

- CPT/CPTU 领域和规范；
- 数值与软件实现；
- Skill 触发与交互；
- 数据、安全和可追踪性；
- 评测设计与统计有效性。

交付物：

- `cpt-interpretation` v1.0；
- 已知限制；
- 支持的方法清单；
- 评测报告；
- 发布 manifest；
- 回归和更新说明。

退出条件：所有重要审查项关闭，无一票否决问题。

### Phase 8：参数反演 Skill

只有 `cpt-interpretation` v1 通过后启动。

研究重点：

- 深度对齐误差；
- 反演参数可辨识性；
- 目标函数和权重；
- 训练/验证隔离；
- 优化稳定性；
- 置信区间和敏感性；
- 跨点位和跨场址验证；
- 禁止超出适用范围外推。

独立门禁：不能只拟合训练集；必须提供独立验证结果、残差和适用范围。

### Phase 9：文献方法复现 Skill

只有公式验证、方法注册和引用合同成熟后启动。

研究重点：

- PDF/文献内容提取可靠性；
- 公式、表格和符号歧义；
- 来源页码和引用；
- 未说明假设；
- 文献算例复现；
- 实现与论文差异；
- 方法 capsule 版本化；
- 防止虚构公式和来源。

独立门禁：没有来源定位和独立测试的方法不得标记为已复现。

## 12. 模型比较研究设计

不同 LLM 的比较不能只看最终文字好坏。建议固定 Skill 和任务，比较：

- 专业正确率；
- Skill 触发准确率；
- reference 选择正确率；
- 脚本调用顺序；
- 用户问题有效率；
- 首次成功率；
- 完成时间；
- token 和成本；
- 失败恢复能力；
- 输出完整性。

实验矩阵示例：

| 模型 | Reasoning | Skill | 任务组 |
| --- | --- | --- | --- |
| 主力高能力模型 | high | off | baseline |
| 主力高能力模型 | high | on | skill |
| 日常平衡模型 | medium | off | baseline |
| 日常平衡模型 | medium | on | skill |
| 低延迟模型 | low/medium | on | repeatable tasks |

优先回答两个问题：

1. Skill 是否让较轻量模型达到无 Skill 高能力模型的质量？
2. 在质量相当时，哪种模型 × reasoning × Skill 组合的时间和成本最低？

## 13. 数据与专家资源

### 13.1 数据要求

- 受控合成数据：覆盖公式和边界；
- 公开或可分发样例：用于 Skill 包和公开测试；
- 营口等真实数据：用于规模和噪声测试；
- 专家持有隐藏数据：用于最终泛化验收；
- 反演配对数据：CPT/CPTU 与室内/现场试验；
- 文献复现数据：论文输入、图表或可重建算例。

### 13.2 专家角色

- 岩土/CPT 专家：知识、适用性和工程语义；
- 数值方法专家：公式、优化、统计和不确定性；
- 软件工程审查：脚本、测试、复现和性能；
- Skill 设计审查：触发、渐进加载和交互；
- 独立评测者：隐藏任务和盲评。

同一人不应同时负责全部金值编写和最终盲评。

## 14. 版本和变更治理

以下对象独立版本化：

- Skill；
- knowledge references；
- formula registry；
- deterministic scripts；
- task set；
- golden vectors；
- evaluation harness；
- model/runtime；
- output schemas。

知识或脚本变化后，旧评测结论不自动继承。至少重跑：

- 相关金值；
- 受影响回归任务；
- 一组隐藏任务；
- 可复现审计。

每次正式发布保存：

- 文件哈希；
- 依赖版本；
- 验证命令；
- 评测摘要；
- 失败和限制；
- 专家签核；
- 支持和不支持的方法范围。

## 15. 主要风险和缓解

| 风险 | 表现 | 缓解 |
| --- | --- | --- |
| Skill 过大 | 触发不准、上下文拥挤 | 三套 Skill 分离，reference 按需加载 |
| 知识过时 | 继续使用旧规范或旧公式 | 来源、版本、审查和更新门禁 |
| 模型现场计算错误 | 数值不稳定 | 关键计算进入测试脚本 |
| 按样例特化 | 开发集很好、真实数据失败 | 隐藏集、真实集和新列名测试 |
| 询问过多 | 使用成本高 | 文件预检优先，只问影响结果的问题 |
| 询问不足 | 静默假设 | 人工确认矩阵和停止条件 |
| Skill 看似提效 | 快但错误或缺成果 | 质量门禁先于效率统计 |
| 结果不可复现 | 同一输入得到不同结果 | 版本、哈希、seed、manifest |
| 模型比较不公平 | 配置和工具不一致 | 固定实验条件和全新会话 |
| 数据泄露 | 日志包含真实数据 | 默认记录元数据和哈希，敏感正文关闭 |

## 16. 项目里程碑

| 里程碑 | 主要结果 | 是否允许进入下一阶段 |
| --- | --- | --- |
| M0 治理冻结 | 目录、版本、隐私和评测 schema | 是 |
| M1 基线完成 | 无 Skill 能力和成本可量化 | 是 |
| M2 知识金值完成 | 公式来源与金值可信 | 是 |
| M3 脚本工具箱完成 | 关键计算确定性通过 | 是 |
| M4 Skill Alpha | 开发集端到端可用 | 是，限前向测试 |
| M5 Skill Beta | 回归和前向测试稳定 | 是，限正式实验 |
| M6 A/B 通过 | 质量与效率增量成立 | 是 |
| M7 v1 发布 | 专家审查和发布 manifest | 可启动参数反演 |
| M8 参数反演通过 | 科学验证和泛化成立 | 可启动文献复现 |
| M9 文献复现通过 | 新方法提取和复现可信 | 完整体系形成 |

## 17. 第一批具体执行任务

在用户确认实施地点后，按以下顺序执行：

1. 选择 Skill 仓库和目录；
2. 建立 `evaluation/` 目录和运行 schema；
3. 从当前仓库提取可复用 CPT 知识清单；
4. 建立 15–20 个无 Skill 基线任务；
5. 执行并保存基线；
6. 建立第一批金值和容差；
7. 实现输入检查、派生计算和 SBT 的确定性脚本；
8. 使用 `skill-creator` 初始化 `cpt-interpretation`；
9. 完成 Alpha 和开发集测试；
10. 完成前向测试和回归；
11. 扩展到 30–40 个正式任务；
12. 执行隐藏集、真实集和模型对照；
13. 完成专家审查；
14. 发布 v1 或根据证据停止发布。

## 18. 停止条件

出现下列情况时停止发布或停止扩大范围：

- 关键知识无法找到可信来源；
- 金值无法独立复核；
- Skill 需要静默确定重大工程假设才能完成；
- 关键计算只能依赖模型自由生成且不能稳定验证；
- 隐藏任务持续出现一票否决问题；
- Skill 未达到质量门禁；
- 达到质量门禁但效率没有优于无 Skill 基线；
- 真实数据授权、隐私或分发边界不清楚；
- 参数反演缺少独立验证数据；
- 文献方法缺少来源定位或验证算例。

停止不等于项目失败。应保留报告，明确问题属于知识、脚本、交互、数据还是模型能力，再决定修订 Skill、缩小范围或更换执行方案。

## 19. 完成定义

第一期完成必须同时满足：

- `cpt-interpretation` Skill v1 已创建并通过结构校验；
- references、scripts、assets 职责清晰且无重复知识；
- 关键公式和单位金值 100% 通过；
- 隐藏任务和真实任务达到发布门槛；
- 无 Skill 与 Skill A/B 结果可重复；
- 质量硬门禁全部通过；
- 效率目标达到或有经确认的调整依据；
- 运行事件、token、模型和成本记录完整；
- 专家审查通过；
- 版本、哈希、限制和支持范围已经归档；
- 参数反演和文献复现的后续接口不需要推翻第一期基础。

## 20. 当前确认结论

- 总体方向：采用专业知识 + 固定流程 + 确定性脚本 + 成果合同的 Skill 体系；
- 总体范围：规划三个 Skills；
- 第一期实施：只发布 `cpt-interpretation`；
- 第一验收原则：先证明做对，再证明提效；
- 第一研究方法：无 Skill 基线、金值、隐藏测试、真实案例、A/B 和专家审查；
- 当前尚未确定：Skill 最终创建位置、第一批外部可分发知识范围、隐藏数据持有人和领域签核人。

Implementation may start：`after confirming the Skill repository/location and evaluation data boundary`。
