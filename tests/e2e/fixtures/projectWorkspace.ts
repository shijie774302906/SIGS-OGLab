import type { ProjectWorkspace } from '../../../src/features/workflow/types';

export function createWorkspace(projectId: string, projectName: string): ProjectWorkspace {
  const pointName = `${projectId}-point`;
  return {
    projectId,
    projectName,
    mode: 'user',
    createdAt: '2026-07-10T00:00:00.000Z',
    updatedAt: '2026-07-10T00:00:00.000Z',
    flowCase: {
      flowId: 'flow-1-data-prep-check',
      scenario: 'valid-with-notice',
      seed: projectId,
      caseId: `CASE-${projectId}`,
      generatedAt: '2026-07-10T00:00:00.000Z',
      sourceType: 'synthetic-csv',
      project: { projectId, projectName },
      point: {
        pointId: pointName,
        pointName,
        pointAlias: 'CPT9-19-S1',
        waterDepthM: 0,
        finalDepthM: 0,
      },
      importBatch: { batchId: `batch-${projectId}`, batchName: `${pointName}.csv` },
      rows: [],
    },
    importDraft: {
      sourceMode: 'project-empty',
      fileName: '尚未导入数据',
      fileType: '未导入',
      status: 'error',
      message: '暂无数据',
      version: 1,
      headers: [],
      rawPreview: [],
      rows: [],
      problems: [],
      pointName,
      filePointNames: [],
      pointDecision: 'matches-current',
      waterDepthM: 0,
      finalDepthM: 0,
      generatedAt: '2026-07-10T00:00:00.000Z',
    },
    selectedMappingField: 'WaterDepthM',
    importFocusField: null,
    checkRunId: `CHECK-${projectId}`,
    checkedDraftVersion: null,
    checkRunHistory: [],
    selectedCheckFilter: 'all',
    flowFeedback: '项目已创建。',
    selection: {
      activeRoute: 'project',
      activeBottomTab: 'issues',
      selectedProjectId: projectId,
      selectedPointId: pointName,
      selectedImportBatchId: `batch-${projectId}`,
      selectedCheckIssueId: '',
      selectedSchemeId: '',
      selectedLayerId: '',
      selectedBoundaryId: '',
      selectedParameterSchemeId: '',
      selectedParameterSlotId: '',
      selectedOutputItemId: '',
    },
  };
}

